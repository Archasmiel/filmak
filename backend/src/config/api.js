// Central API configuration (versioning / base prefix)
// Customize via API_PREFIX env var, defaults to /api/v1 for forward compatibility.
export const API_PREFIX = process.env.API_PREFIX || '/api/v1';
